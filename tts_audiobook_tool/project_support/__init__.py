"""
Project-centered support modules.

This package groups operational helpers and file/artifact management code that
surround the core `Project` model without moving `project.py` itself out of the
root package. The main purpose is to reduce root-package sprawl while keeping
project-specific support logic easy to find.
"""