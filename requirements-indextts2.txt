# ################
# Use Python v3.11
# ################

# Most recent commit of IndexTTS2.
# Use GitHub's commit source archive instead of a git+https requirement because
# upstream tracks example WAVs in Git LFS and their LFS quota is often exceeded.
# A normal pip git clone then fails during checkout/smudge before installation,
# even though the app does not need those example files. The archive URL keeps
# the same pinned commit while avoiding Git LFS downloads.
indextts @ https://github.com/index-tts/index-tts/archive/db5b39bb6ad903c219b2dd33d60b0f0bdaede664.zip
ninja

# App dependencies
faster-whisper==1.2.1; sys_platform != 'darwin' or platform_machine != 'arm64'
ctranslate2==4.7.1; sys_platform != 'darwin' or platform_machine != 'arm64'
mlx-whisper; sys_platform == 'darwin' and platform_machine == 'arm64'
git+https://github.com/zeropointnine/tts-audiobook-tool-sidon; sys_platform == 'linux' and platform_machine == 'x86_64'

torch==2.8.0

audiotsm==0.1.2
chardet==5.2.0
beautifulsoup4==4.12.3
EbookLib==0.18
ffmpeg-python==0.2.0
hf_xet
librosa
loguru==0.7.3
metaphone==0.6
mutagen==1.47.0
natsort==8.4.0
num2words==0.5.14
nvidia-ml-py
psutil
pydantic
pyloudnorm==0.1.1
pysbd==0.3.4
requests
pywin32; sys_platform == 'win32'
sounddevice==0.5.2
soundfile==0.13.1
watchdog==6.0.0
whisper_normalizer==0.1.12
text2num==3.0.1
xxhash==3.5.0
